name: Quality checks
permissions:
  contents: read

on:
  push:
  pull_request:

jobs:
  quality:
    name: Lint, typecheck, tests, and build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Check formatting
        run: npm run format:check

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Run unit tests
        run: npm run test:ci

      - name: Production build
        run: npm run build

      - name: Upload results to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}

  accessibility:
    name: Browser accessibility smoke
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Install browser engines
        run: npx playwright install --with-deps chromium webkit

      - name: Run WCAG smoke
        run: npm run test:e2e:a11y

      - name: Run cross-browser release smoke without browser MSW
        run: npm run test:e2e:smoke:matrix

      - name: Run production release smoke
        run: npm run test:e2e:smoke:production

  visual:
    name: Visual regression matrix
    runs-on: macos-14
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Install browser engines
        run: npx playwright install chromium webkit

      - name: Run responsive visual regression
        run: npm run test:e2e:visual
